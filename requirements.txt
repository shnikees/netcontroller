# Core pipeline
faster-whisper>=1.0.0
webrtcvad>=2.0.10
# webrtcvad still does `import pkg_resources` at import time, which setuptools
# removed in 81. Drop this pin once webrtcvad ships a release without it.
setuptools<81
sounddevice>=0.4.6
numpy>=1.24
# High-quality resampling for inputs that will not do 16 kHz (mic, line in).
# Optional: a slower built-in fallback runs if this is missing.
soxr>=0.3
# High-pass filtering before decoding. Optional: falls back to DC removal.
scipy>=1.11
rapidfuzz>=3.6
PyYAML>=6.0
# CPU and memory for the dashboard's status strip. Optional: the load
# average alone still answers 'is this box saturated?' without it.
psutil>=5.9

# Web dashboard
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0

# Tests
pytest>=8.0
httpx>=0.27          # fastapi.testclient
